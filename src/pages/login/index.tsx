import LoginInterface from "@/components/Account/logintemplate";

const LoginPage = (): JSX.Element => {
  return (
    <div className="account">
      <LoginInterface />
    </div>
  );
};

export default LoginPage;